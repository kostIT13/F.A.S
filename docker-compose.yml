services:
  vector-search:
    build: .
    container_name: vector-search
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./src:/app/src
    environment:
      - COLLECTIONS_PATH=/app/data
      - PYTHONUNBUFFERED=1
    restart: unless-stopped